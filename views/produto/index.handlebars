<div class="col">
    <div class="row">
        <div class="col-6">
            <img src="{{produto.imagem}}">
        </div>
        <div class="col-6">
            <h2 class="text-center">{{produto.nome}}</h2>
            <hr>
            <h3 class="text-end text-success">{{produto.preco}}</h3>
        </div>

        <div class="d-grid justify-content-lg-end mb-5">
            <a href="/usuarios/carrinho/carrinho-add/{{produto._id}}" class="btn btn-lg btn-success">Comprar</a>
        </div>

    </div>
</div>